# 2026-04-24 工作日志

## 记录 1 — ILP 投连险功能完整集成

- **[Status]**: 已完成
- **[Change Log]**:
  1. `apps/fis-hub/src/components/ILPConfigPanel.jsx` — 🆕 新建
     - 完整 COI 保险费用率表（男/女，0-80岁）
     - 开户奖赏推广率查表函数 `getEnrollmentBonusRate()`
     - 累进长期客户奖赏计算 `calcLoyaltyBonus()`
     - 月度 ILP 净现金流主函数 `calcILPMonthly()`（供 MC 模拟调用）
     - UI：受保人信息输入（年龄/性别/吸烟/整付保费/货币）
     - 实时计算摘要：身故赔偿、开户奖赏、费用年化、长期奖赏预览

  2. `apps/fis-hub/src/components/ILPEnrollmentBonusModal.jsx` — 🆕 新建
     - 开户奖赏确认弹窗，系统自动匹配推广档位
     - 推广率参考表一览（$50k→2.5%, $100k→3.5%, $300k→4.5%, $500k→5.5%, $1M→6%）
     - 支持手动调整奖赏率（0-10%），实时计算奖赏金额
     - 推广期提示（2026/4/1-6/30，仅香港）

  3. `apps/fis-hub/src/components/ILPSummaryCard.jsx` — 🆕 新建
     - 通用卡片组件，支持 compact（PortfolioView）和完整模式（StrategyLabView）
     - 保障功能：身故赔偿进度条（CV vs 保额）
     - 传承功能：免遗产认证、直接赔付受益人、不受债权人追索
     - 费用状态 + 奖赏状态双栏展示

  4. `apps/fis-hub/src/App.jsx` — ✏️ 修改
     - 新增 import ILPEnrollmentBonusModal
     - 新增 state：ilpEnabled, ilpConfig, ilpEnrollmentModalOpen
     - 向 StrategyLabView 传递 ILP props
     - 向 PortfolioView 传递 ILP props
     - Modals 区域渲染 ILPEnrollmentBonusModal

  5. `apps/fis-hub/src/components/StrategyLabView.jsx` — ✏️ 修改
     - import ILPConfigPanel + ILPSummaryCard
     - props 新增 ilpEnabled, setIlpEnabled, ilpConfig, setIlpConfig, onOpenIlpEnrollmentModal
     - Block 1 末尾新增 ILP Toggle 开关 + ILPConfigPanel 展开区域
     - MC 图表下方新增 ILPSummaryCard（完整模式）

  6. `apps/fis-hub/src/components/PortfolioView.jsx` — ✏️ 修改
     - import ILPSummaryCard
     - props 新增 ilpEnabled, ilpConfig, onOpenIlpEnrollmentModal
     - Holdings Tab 顶部新增 ILPSummaryCard（紧凑模式）

- **[Logic Reason]**: 用户需求：ILP 作为波动组合的实现载体，费用自动从回报中扣减，叠加奖赏收益。所有计算函数通过 export 供后续 MC 模拟直接调用。
- **[Pending/TODO]**: 
  - MC 模拟引擎（后端）接入 ILP 费用/奖赏计算（前端函数已就绪，需同步后端逻辑）
  - 前端 MC 对比线（有/无 ILP 两条曲线）尚未实现（需后端支持 ilpEnabled 参数）

---

## 记录 2 — ILP 配置持久化架构升级

- **[Status]**: 已完成
- **[Change Log]**:
  1. `backend/models/fip.py` — 新增 `UserILPConfig` SQLAlchemy 模型
  2. `backend/routers/fip_router.py` — 新增 ILP 配置 API（GET/PUT /api/ilp/config，GET/PUT /api/ilp/config/{user_id}）+ 自动建表
  3. `ILPSettingsModal.jsx` — 新建通用 ILP 配置弹窗（普通用户 / Advisor 双模式）
  4. `Sidebar.jsx` — 底部新增「ILP 投连险配置」按钮，登录后显示
  5. `AdvisorClientsPanel.jsx` — 客户展开区域新增「ILP 投连险配置」按钮
  6. `App.jsx` — 登录后自动从后端加载 ILP 配置并同步 state
- **[Logic Reason]**: 将 ILP 配置从会话临时 state 升级为数据库持久化，用户级别全局生效；Advisor 可替客户保存配置
- **[Pending/TODO]**: 无

---

## 记录 3 — ILP 费用月度影响计算与可视化

- **[Status]**: 已完成
- **[Change Log]**:
  1. `apps/fis-hub/src/utils/ilpImpactUtils.js` — 新建 ILP 费用计算引擎（COI查表、开户奖赏、长期客户奖赏、逐月净费用、MC曲线后处理）
  2. `apps/fis-hub/src/components/ILPImpactPanel.jsx` — 新建 ILP 费用影响可视化面板（双Tab：对比图/年度明细表）
  3. `apps/fis-hub/src/components/StrategyLabView.jsx` — 在MC图表区域下方渲染 ILPImpactPanel
- **[Logic Reason]**: 前端后处理方案，无需改动后端MC引擎。对P10/P50/P90逐月扣减ILP净费用，生成含ILP和不含ILP的对比曲线
- **[Pending/TODO]**: 若需要更精确的模拟（每条路径独立扣费），需后端MC引擎集成ILP参数

---

## 记录 4 — ILP 组合 View 历史曲线修正

- **[Status]**: 已完成
- **[Change Log]**:
  1. `PortfolioView.jsx` — 修正 ILP 历史曲线三处逻辑：
     - 保费计算基准改为 `historyData[0].value`（组合初始投入），与组合对齐
     - 初始 ILP 倍数 = `1 + bonusRate`，期初即反映开户奖赏（单位方式计入）
     - 摘要条新增「🎁 开户奖赏」格和说明脚注
  2. `App.jsx` — ILP ↔ MC 双向联动（3个useEffect：开启/capital变/保费变）
  3. `StrategyLabView.jsx` — Capital 紫色联动标识，Annual Add 锁定为0
- **[Logic Reason]**: 
  - 旧逻辑用 ilpConfig.totalPremium 作账户价值基准，可能与组合初始值不一致
  - 旧逻辑 ILP 曲线起点与原始曲线同为 firstVal，遗漏了开户奖赏
- **[Pending/TODO]**: 无
