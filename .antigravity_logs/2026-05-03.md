## [2026-05-03] 支出凭证财务年度管理

**[Status]** 已完成

**[Change Log]**
  - `apps/company-admin/src/pages/Expenses.jsx`: 增加财政年度编辑表单，增加“未设定财务年度”快捷筛选。
  - `backend/routers/expenses.py`: 增加财政年度修改支持，并支持查询 `fiscal_year == None` 的记录。

**[Logic Reason]**
用户要求能够手动编辑财务年度，并能快速筛选出 AI 未能自动匹配财务年度的记录（即 `fiscal_year` 为 `None` 的数据）。通过前端传入特定的 `fiscal_year: 'none'`，让后端识别并将其转换为 `fiscal_year IS NULL` 的查询条件。在编辑表单中传入并处理 `fiscal_year`，若传空字符串则清理该字段为 null，使其符合后端逻辑。

**[Pending/TODO]**
无
