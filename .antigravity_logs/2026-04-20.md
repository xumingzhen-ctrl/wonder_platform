# 2026-04-20 变更日志

## Dividend Fetching Logic Optimization

**[Status]** 已完成

**[Change Log]**
- 修改 `services/fip/data_provider.py`: 移除 `get_dividend_history` 中对 `LU/IE` ISINs 的强制阻断逻辑。
- EODHD 未配置时自动回退至 yfinance。

**[Logic Reason]**
实测 yfinance 可提供 LU1128926489 的完整分红历史（49 条），不应硬编码阻断。

---

## Financial API Infrastructure Upgrade

**[Status]** 已完成

**[Change Log]**
- 修改 `.env`: 配置 `EODHD_API_KEY` 和 `FMP_API_KEY`。
- 修改 `config.py`: 在 Settings 类中声明新字段。

---

## 凭证识别崩溃修复（BUG-HOTFIX）

**[Status]** 已完成

**[Change Log]**
- 修改 `services/receipt_scanner.py` 的 `generate_voucher_number`。
- 新增 `re.match(r"^\d+$", suffix)` 过滤非标准凭证号后缀。

**[Logic Reason]**
历史遗留 `EXP-YYYYMM-M001` 格式导致 `int("M001")` 抛出 ValueError。

---

## Email Verification & Password Reset (Full Platform)

**[Status]** 已完成

**[Change Log]**
- Backend: `User` 增加 `is_verified`，新增 `UserToken` 模型，新增 `/forgot-password`, `/reset-password`, `/verify-email` 接口。
- Frontend (company-admin): 新增 VerifyEmail, ForgotPassword, ResetPassword 页面。
- Frontend (fis-hub): AuthModal 增加找回密码模式，新增 AuthOverlays 组件。

**[Logic Reason]**
通过 `redirect_url` 参数支持多前台的邮件跳转链接。

---

## 修复认证弹窗点击空白关闭

**[Status]** 已完成

**[Change Log]**
- `apps/fis-hub/src/components/AuthModal.jsx`: 移除遮罩层 `onClick={onClose}`。

---

## 修复 "欢迎回来, undefined"

**[Status]** 已完成

**[Change Log]**
- `apps/fis-hub/src/App.jsx`: 为 `currentUser.display_name` 添加级联回退逻辑。
