
### 2026-05-13 15:20 - Fix display_name mismatch in fis-hub

- **[Status]**: 已完成
- **[Change Log]**:
  - `apps/fis-hub/src/components/AdminPanel.jsx`：将过滤和渲染逻辑中的 `display_name` 替换为 `name`。
  - `apps/fis-hub/src/components/AuthModal.jsx`：注册和状态保存逻辑中的 `display_name` 替换为 `name`。
  - `apps/fis-hub/src/components/AdvisorClientsPanel.jsx`：将客户卡片中的 `display_name` 替换为 `name`。
  - `apps/fis-hub/src/App.jsx`：导航栏欢迎语中的 `display_name` 替换为 `name`。
  - `backend/routers/fip_router.py`：全局将 `display_name` 替换为 `name`，与数据库 Schema 完全一致。
- **[Logic Reason]**: 后端数据库 schema (`users`表) 实际字段是 `name`。而 `fis-hub` 前端组件以及部分后端遗留查询仍期待使用 `display_name`，导致由于 API 返回 `name` 造成字段缺失，管理员后台显示“—”而不是用户的姓名。通过统一将其重构为 `name` 彻底解决姓名显示缺失和后台数据无法对齐的问题。
- **[Pending/TODO]**: 检查部署后管理员界面的姓名显示效果，确保无其它遗漏。
