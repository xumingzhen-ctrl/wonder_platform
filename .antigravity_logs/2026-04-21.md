# 2026-04-21 变更日志

## 文章管理体系搭建完成

**[Status]** 已完成

**[Change Log]**
- 新增 `articles_inbox/` 投稿文件夹（含 README）
- 新增 `convert_articles.py` 自动转换脚本（Word → Markdown）
- 新增 `apps/wonder-hub/src/lib/constants.ts`（共享常量）
- 新增 `apps/wonder-hub/src/lib/useAuth.ts`（客户端 auth hook）
- 修改 `blog.ts`: 添加 restricted 字段
- 修改 `blog-list.tsx`: auth 感知 + 客户通讯锁屏 Banner
- 修改 `[slug]/page.tsx`: 服务端 restricted 访问保护
- 批量转换 28 篇文章；修复 6 篇客户通讯分类错误；删除 2 篇重复文章

**[Logic Reason]**
RESTRICTED_CATEGORY 常量从独立 constants.ts 导出，避免 "use client" 组件导入 server-only 模块导致 500。

---

## 文章批量转换 v2.0（多次迭代）

**[Status]** 已完成

**[Change Log]**
- 使用 convert_articles.py v2.0 转换全部 43 篇文章（含 85 张图片）
- 图片提取保存至 `public/article-images/<slug>/`，优先 WebP 格式
- 表格：HTMLParser 解析 th/td，输出对齐 Markdown 表格
- 客户通讯正确标记 `restricted: true`

---

## 修复颜色对比度 + 升级转换脚本

**[Status]** 已完成

**[Change Log]**
- `globals.css`: Solarized Light → 高对比度温暖白主题（foreground #657b83 → #1a1a1a）
- 新增 `.article-body` 完整排版样式（段落/标题/列表/引用/表格/图片）
- `convert_articles.py` v2.0: 图片提取 + WebP 转换 + 表格解析

---

## 登录系统 + 客户通讯访问控制重构

**[Status]** 已完成

**[Change Log]**
- 新增 `TopNav.tsx`: 全局导航栏（登录按钮、用户头像菜单）
- 新增 `TopNavWrapper.tsx`: layout 注入用 client wrapper
- 修改 `layout.tsx`: 全局注入 TopNavWrapper
- 重构 `blog-list.tsx`: 客户通讯卡片摘要公开，全文需登录
- 重构 `[slug]/page.tsx`: 三层访问控制（微信 token → JWT cookie → 拦截门）
- 新增 `ArticleLoginGate.tsx`: 文章内嵌登录表单
- 新增 `gen_wechat_link.py`: 微信专属链接生成工具

**[Logic Reason]**
摘要公开增加内容吸引力，全文通过 JWT / 微信 token / 服务端守卫三条通道控制。

---

## 格式修复 + Token 永久化 + 摘要优化

**[Status]** 已完成

**[Change Log]**
- Token 改为永久有效（base64url 格式）
- `globals.css`: 全面重构 article-body 排版（表格 hover/斑马纹/溢出滚动）
- `[slug]/page.tsx`: 加入 remarkGfm + remarkBreaks 插件
- `convert_articles.py`: 新增 extract_newsletter_performance() 函数

**[Logic Reason]**
remark-gfm 插件未加载导致 Markdown 表格被当普通文字渲染。

---

## GFM 表格渲染根本修复

**[Status]** 已完成

**[Change Log]**
- `convert_articles.py`: 新增 `fix_markdown_tables()` 后处理函数
- 检测嵌套在列表项中的表格行，提取到列表外
- 确保表格块前后有空行（GFM 规范）

**[Logic Reason]**
Word 编号段落 → mammoth `<li>` → Markdown `- | col |` → remark-gfm 不解析列表内表格。
