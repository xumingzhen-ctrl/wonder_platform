# Antigravity Global Log

## Initial Setup
- **Status**: 进行中
- **Change Log**: 创建了根级 `.antigravity_log.md` 以遵从全局日志约束。
- **Logic Reason**: 为后续的 Progressive Monorepo 大重构提供唯一且不可变的日志追踪枢纽。
- **Pending/TODO**: 编写并提交 Phase 1 物理目录重组方案等待 User 批准。

## Phase 1 物理目录重组
- **Status**: 已完成
- **Change Log**: 
  1. 创建 `apps/` 目录，将 `WONDER HUB` 移入为 `apps/wonder-hub`，将 `Company Admin/frontend` 移入为 `apps/company-admin`。
  2. 提取 `Company Admin/backend` 为根级 `/backend`，并将 `Financial Information Publist/backend` 内的核心引擎文件和 `plugins`/`core` 目录移动至 `/backend/services/fip`。
  3. 合并 FIP 的 Python 依赖到根 `/backend/requirements.txt` 并删除了空壳 `Company Admin` 目录。
  4. 初始化了 pnpm workspaces 配置文件（`package.json`, `pnpm-workspace.yaml`, `turbo.json`）及全局统一的 `docker-compose.yml`。
- **Logic Reason**: 按照前端分离（pnpm workspaces + Turborepo）、后端大一统（共享单一 FastAPI + requirements）的 Monorepo 策略，完成了基础设施的物理结构划分，为后续的合并代码及构建流程打下基础。
- **Pending/TODO**: 配置前后端服务的联调，将 FIP 的路由注册到主 `main.py` 并接入现有的 User/RBAC JWT 鉴权体系。

## Phase 2 后端大一统与鉴权融合
- **Status**: 已完成
- **Change Log**: 
  1. 在 `backend/models/fip.py` 中重构并注册了 FIP 专属的 SQLAlchemy 表结构。
  2. 统一了 FIP 代码库中的 `DB_PATH`，全部指向根级别的 `hk_admin.db`，实现物理数据库融合。
  3. 将 FIP 庞大的 `api_server.py` 重构为 `backend/routers/fip_router.py`，并将全部 `FastAPI` 实例转为 `APIRouter`。
  4. 采用桥接模式 (Adapter Pattern)，将 FIP 原有的 `get_current_user` 无缝对接至 Company Admin 的 SQLAlchemy User 鉴权逻辑中。
  5. 在 `venv` 环境补齐了 `pandas`, `yfinance`, `requests-cache` 等相关依赖并成功跑通了整个后端挂载测试。
- **Logic Reason**: 避免对 FIP 的底层复杂 SQL 逻辑进行大规模破坏性重写。通过依赖桥接技术，在零业务逻辑代码修改的前提下，完美接入了行政后台的统一 RBAC 登录态。
- **Pending/TODO**: 前端跨包/应用的联合调用测试与 Docker 部署编排验证。

## Phase 3 前端联调与工作区 (Workspace) 跑通
- **Status**: 已完成
- **Change Log**:
  1. 通过 `npx pnpm install` 全局初始化了 Root 层级的 `node_modules`，自动串联了 `wonder-hub` 和 `company-admin` 的依赖。
  2. 在根 `package.json` 的 scripts 中注入了 `npx pnpm exec turbo run dev` 的全量兼容命令。
  3. 检查了 `company-admin` 下的 `vite.config.js`，确认 `/api` 及 `/receipts` 的代理规则精准对齐到了大一统后端的 `http://localhost:8000` 且开启了跨域。
  4. 利用 Turbo 成功并发启动了双子星前端：`wonder-web` (Next.js @ port 3000) 和 `hk-admin-frontend` (Vite @ port 5174)。
- **Logic Reason**: 为实现 “前端分离，后端大一统”的 Progressive Monorepo，成功通过 Turbo 结合 PNPM Workspace 达成了对两套技术栈（Next.js SSR vs React SPA）的一键同轨运行能力，规避了依赖地狱。
- **Pending/TODO**: 最后一块拼图：编写及重构 Dockerfile 与 `docker-compose.yml`，以容器化方式发布这个巨型全栈应用！

### 2026-04-18 (System Stability - OOM Hotfix)
- **[Status]**: 已完成
- **[Change Log]**:
  - `apps/wonder-hub/src/app/globals.css` [MODIFY] —移除了导致 Next.js Turbopack 无限循环解析的非法不存在样式导入 (`shadcn/tailwind.css` 和 `tw-animate-css`)。
  - `Wonder_Platform/package.json` [MODIFY] — `dev` 命令中注入了 `NODE_OPTIONS='--max-old-space-size=4096'` 防止 Node 内存溢出死锁。
  - Workspace [MODIFY] — 通过 `pnpm add -w tailwindcss @tailwindcss/postcss` 修复了由于 strict-peer-dependency 导致的 Tailwind v4 无法被 Turbopack 正确 resolve 的问题。
  - `apps/wonder-hub/.next` [DELETE] — 清理被污染的旧编译缓存。
- **[Logic Reason]**: Turbopack 在 pnpm 的严格依赖隔离树中，一旦遇到不存在或非依赖声明列表中的 `@import` 文件，会递归扫描上级 `node_modules` 导致死循环，这瞬间耗尽了 Mac 内存引发全局死锁。清除无效代码并将 `tailwindcss` 提升为根目录级依赖彻底打通了解析。
- **[Pending/TODO]**: 后续可在正式环境中复查 Tailwind v4 的 CSS Tree Shaking 是否完备。

### 2026-04-18 (Integration - FIS Frontend Monorepo Migration)
- **[Status]**: 已完成
- **[Change Log]**:
  - `Financial Information Publist/frontend` [MOVE] — 物理迁移至 `apps/fis-hub`，正式作为第三个微前端并入 Turborepo 工作区。
  - `apps/fis-hub/package.json` [MODIFY] — 更名为 `fis-hub`，并在根目录触发 `pnpm install` 实现依赖提升。
  - `apps/fis-hub/vite.config.js` [MODIFY] — 绑定独立端口 `5175`，并增加对 `http://localhost:8000/api` 的反向代理配置。
  - `apps/wonder-hub/src/app/page.tsx` [MODIFY] — 在首屏功能矩阵中新增第五个卡片入口【FIS 系统】，指向外部独立访问链接。
  - `apps/wonder-hub/src/app/assessment/page.tsx` [MODIFY] — 将“进入深度沙盘系统”按钮重构为 `<a>` 标签，打通跳转 `http://localhost:5175` 的流程。
- **[Logic Reason]**: 前端架构补齐。采用了“方案A：外部跳转”策略，保持了各系统内部路由的原子性和纯净性，降低了单页应用嵌套引入的隐性 Bug。
- **[Pending/TODO]**: 测试 FIS 内的登录验证状态（JWT Token 隔离）是否与后台跨域冲突或能正常共享。

### 2026-04-18 (Bug Fix - CORS & Vite Build Crash)
- **[Status]**: 已完成
- **[Change Log]**:
  - `backend/main.py` [MODIFY] — 在 FastAPI 的 `CORSMiddleware` 的 `allow_origins` 列表中补齐了 `http://localhost:5174` 和 `http://localhost:5175`。
  - `apps/company-admin/src/index.css` [MODIFY] — 修复了导致整个 `npx pnpm dev` 崩溃的致命构建错误，将 `@import './fin_styles.css'` 从文件底部提升至第一行，以符合 Vite 5.4 的标准。
- **[Logic Reason]**: 
  1. 第二张截图的跨域报错是因为 FIS 代码中写死了直接请求后端 `http://localhost:8000`，但由于端口是新分配的 `5175`，被 FastAPI 的跨域防火墙拦截。通过补齐白名单实现秒修。
  2. 终端的异常终止是因为子项目 `company-admin` 的 CSS 不规范触发了 Vite 编译失败。修复后 `turbo` 能够顺利挂载所有前端服务。
- **[Pending/TODO]**: 建议后续有时间将 FIS 代码中的 `http://localhost:8000` 统一替换为 `/api`，以匹配正式生产环境的 `docker-compose` 代理架构。

### 2026-04-18 (Database Seeding)
- **[Status]**: 已完成
- **[Change Log]**:
  - `backend/seed_admin.py` [CREATE] — 编写了一次性 Python 脚本，直接连通 SQLAlchemy 对 `hk_admin.db` 写入超级管理员账户。
  - `Database` [INSERT] — 在 `User` 表中生成了账号 `admin@wonderhub.hk`。
- **[Logic Reason]**: 绕过前端注册限制，通过后台脚本直接在生产/测试数据库中植入带有完全权限 (role="admin") 及正确哈希密码的种子用户，方便直接登录各项 SaaS 系统。
- **[Pending/TODO]**: 确认新建的账号可以顺利登录 `FIS` 和 `Company Admin` 平台。

### 2026-04-18 (Bug Fix - Routing 404)
- **[Status]**: 已完成
- **[Change Log]**:
  - `backend/routers/fip_router.py` [MODIFY] — 移除了 `prefix="/fip"` 的 APIRouter 路由前缀。
- **[Logic Reason]**: 此前的重构将 FIS 后端引擎化为了 `fip_router` 并加上了 `/fip` 前缀。但由于 FIS 前端代码仍在大范围硬编码直接调用根路径 API（如 `http://localhost:8000/portfolios`），导致所有请求全部触发 404，进而引发前端因数据结构类型不匹配 (`map is not a function`) 造成的白屏崩溃。通过移除后端的前缀，使接口路径重新对齐前端调用。
- **[Pending/TODO]**: 无。系统现已可完全正常流转。

### 2026-04-18 (Data Migration - FIP Ownership)
- **[Status]**: 已完成
- **[Change Log]**:
  - `backend/migrate_fip_data.py` [CREATE] — 编写了一次性迁移脚本。
  - `Database` [INSERT] — 将遗留的 `Financial Information Publist/backend/portfolio.db` 中的所有历史数据（包括 5 个实盘组合、6 个实验室沙盘方案、缓存与交易流水等）安全迁入了大一统的 `hk_admin.db`。
  - `Database` [UPDATE] — 在迁移期间动态为 `lab_scenarios` 表扩充了 `user_id` 字段，并将所有迁移过来的 `portfolios` 和 `lab_scenarios` 记录的 `user_id` 全部显式绑定到了 `admin@wonderhub.hk` 账号的 UUID 下。
- **[Logic Reason]**: 此前的架构融合虽然打通了物理文件，但没有迁移 SQLite 内部的实际业务数据。为了响应要求，我提取了旧的隔离版 FIP 数据库，在执行跨库迁移的同时，对数据所有权进行了清洗与赋权，确保新注册的超级管理员一登录就能直接看到之前辛辛苦苦建立的全部金融推演数据。
- **[Pending/TODO]**: 建议后续进一步在 API 层（如 `GET /portfolios`）落实严格的 `WHERE user_id = ?` 隔离查询。

### 2026-04-18 (Data Update - Portfolio Visibility)
- **[Status]**: 已完成
- **[Change Log]**:
  - `Database` [UPDATE] — 执行 SQL 脚本，将 `portfolios` 表中的 `Accumulation Portfolio` 和 `Income Strategy Sample` 两个组合的 `is_public` 标志位统一设置为 `1`。
- **[Logic Reason]**: 按照用户要求，将指定的测试与展示沙盘组合设置为“全员可见”状态，以便无论后续是哪种角色的账户登录，都能以此作为示例数据进行浏览或基础操作。
- **[Pending/TODO]**: 后续如果在 API 层面落实了 `WHERE user_id = ? OR is_public = 1`，这两条数据即可真正实现跨账户共享。

### 2026-04-18 (Bug Fix - SQL Schema Mismatch)
- **[Status]**: 已完成
- **[Change Log]**:
  - `backend/routers/fip_router.py` [MODIFY] — 在 `advisor_list_clients` 路由的 SQL 查询中，将旧版 FIP 数据库的 `u.display_name` 别名映射改写为了适应现版大一统 `users` 表结构的 `u.name as display_name`。
- **[Logic Reason]**: 在后端热重载时触发了 `sqlite3.OperationalError: no such column: u.display_name` 崩溃，这是因为 FIP 遗留 SQL 语句与行政后台的 `User` 表结构（字段名为 `name`）不匹配所致。通过 SQL `AS` 别名修复以兼顾底层结构与前端渲染。
- **[Pending/TODO]**: 无。请重新启动开发服务器。

### 2026-04-18 (Repo Initialization & Push)
- **[Status]**: 已完成
- **[Change Log]**:
  - `.gitignore` [CREATE] — 创建了基础的 Git 忽略规则文件。
  - `Workspace` [INITIALIZE] — 将当前工作区初始化为 Git 仓库，设置远程地址为 `https://github.com/xumingzhen-ctrl/wonder_platform.git` 并推送全部代码。
- **[Logic Reason]**: 响应用户需求，将整个本地代码库同步推送到了新建立的远程仓库进行版本控制。
- **[Pending/TODO]**: 警告：在此过程中发现部分子目录（如 `Financial Information Publist` 和 `apps/wonder-hub`）可能被添加为了内嵌仓库 (embedded git repo)。如有必要，后续需清理这些子项目的 `.git` 文件夹并重新提交以构建统一的 monorepo。

### 2026-04-18 (Repo Conversion - Monorepo Fix)
- **[Status]**: 已完成
- **[Change Log]**:
  - `apps/wonder-hub/` [UPDATE] — 移除了子目录中的 `.git` 文件夹并清除了 Git 缓存引用。
  - `Financial Information Publist/` [UPDATE] — 移除了子目录中的 `.git` 文件夹并清除了 Git 缓存引用。
  - `Workspace` [COMMIT & PUSH] — 将上述子项目作为正常目录重新提交，并推送到远程 `main` 分支。
- **[Logic Reason]**: 修复由于子模块/内嵌仓库导致的空壳引用问题，确保所有前端项目与后端项目被无缝合并为大一统 (Monorepo) 代码库。
- **[Pending/TODO]**: 无，代码库迁移与推送任务已彻底完成。

### 2026-04-18 (Docker Config - Synology NAS Setup)
- **[Status]**: 已完成
- **[Change Log]**:
  - `apps/wonder-hub/Dockerfile` [CREATE] — 创建了用于群晖的 Next.js 生产环境构建文件（启用 Node server 以支持 API Routes）。
  - `apps/company-admin/Dockerfile` & `nginx.conf` [CREATE] — 创建了 Vite 项目的构建文件，并配置 Nginx 以代理 `/api` 和 `/receipts` 请求到后端。
  - `docker-compose.prod.yml` [CREATE] — 创建了群晖专用的生产编排文件。端口打乱为：Backend(8123), Wonder Hub(3123), Company Admin(5273)。配置了 `/volume1/.../data` 目录的数据持久化。
- **[Logic Reason]**: 用户需要在群晖 NAS (Container Manager) 上部署生产环境。为防止端口冲突对端口进行了乱序处理，为防止 SQLite `-wal` 文件丢失而改为挂载 `db` 文件夹，并基于前端架构差异 (Next.js API vs Vite Static) 分别采用 Node 与 Nginx 进行部署。
- **[Pending/TODO]**: 部署到群晖时，需要确保在 `/volume1/docker/wonder_platform/data/` 下手动创建一个 `db` 空文件夹，系统启动后会自动生成 `hk_admin.db`。

### 2026-04-18 (Bare Metal Scripts)
- **[Status]**: 已完成
- **[Change Log]**:
  - `start_prod.sh` [CREATE] — 编写了一键在后台裸机(nohup)启动前后端的脚本。
  - `stop_prod.sh` [CREATE] — 编写了一键杀死后台相关进程的停止脚本。
- **[Logic Reason]**: 用户希望放弃 Docker，寻求最简单的单行命令以在后台永久挂载所有服务。利用 vite preview 保留了原有的 /api 代理逻辑，完美替代了 Nginx 的作用。
- **[Pending/TODO]**: 运行前需确保 Mac 或群晖上已经运行过了 npm run build 进行静态文件构建。

### 2026-04-18 (System Stability - SQLite Segmentation Fault Hotfix)
- **[Status]**: 已完成
- **[Change Log]**:
  - `backend/services/fip/data_provider.py` [MODIFY] — 将 `requests_cache.install_cache` 的 `backend` 从隐式的 `sqlite` 修改为 `memory`。
  - `backend/services/fip/api_response_cache.sqlite` [DELETE] — 清理遗留的磁盘缓存文件。
- **[Logic Reason]**: 修复由于 `requests_cache` 使用全局 SQLite 猴子补丁，在 FastAPI 多线程高并发环境下导致的 Python 解释器 `Segmentation fault`（段错误/进程意外退出）崩溃。改用内存缓存后速度更快且彻底避开 SQLite 的并发锁死风险。
- **[Pending/TODO]**: 确认前端 FIS Dashboard 在重新启动后是否稳定，不再触发崩溃白屏。

### 2026-04-18 (Docker & Configuration Refactoring)
- **[Status]**: 已完成
- **[Change Log]**:
  - `backend/Dockerfile` & `start_prod.sh` [MODIFY] — 将暴露和运行的后端端口从 8000 改为 1234。
  - `docker-compose.yml` & `docker-compose.prod.yml` [MODIFY] — 更新容器端口映射（如 8123:1234），并将生产编排文件中的绝对数据挂载路径 (`/volume1/docker/...`) 修正为相对路径 (`./data/...`)。
  - `apps/company-admin/vite.config.js`, `apps/company-admin/nginx.conf`, `apps/fis-hub/vite.config.js` [MODIFY] — 将全部前端代理及静态资源请求的目标端口由 8000 同步修改为 1234。
  - `settings.json` [CREATE] — 在项目根目录创建全局配置文件，取代 `.env`。
  - `backend/config.py` [MODIFY] — 新增 JSON 解析逻辑，在启动前读取 `settings.json` 并注入环境变量。
- **[Logic Reason]**: 
  1. 用户要求统一将系统后端 API 的入口由默认的 8000 修改为自定义的 1234。
  2. 收拢所有配置变量到统一的根级 `settings.json` 文件中，便于全局维护。
  3. 修复 Docker 持续重启问题：通过诊断发现此前的 Synology 绝对挂载路径 (`/volume1/...`) 在本地启动时会导致目录无法找到、SQLite -wal 文件无法持久化并最终引起 Uvicorn 崩溃。改用 `./data/` 相对路径保证了容器内目录自动生成和挂载的跨环境可用性。
- **[Pending/TODO]**: 请用户确认此方案并进行实际构建运行 (`docker-compose up -d --build`) 测试。

### 2026-04-18 (Deployment - Alibaba Cloud Lighthouse + PM2)
- **[Status]**: 已完成
- **[Change Log]**:
  - `start_prod.sh` [MODIFY] — 补充启动 fis-hub（Port 5175），修正 company-admin 端口为 5174，新增访问地址汇总输出。
  - `stop_prod.sh` [MODIFY] — 更新说明，`pkill -f "vite preview"` 已同时覆盖 company-admin 和 fis-hub 两个进程。
  - `docker-compose.prod.yml` [MODIFY] — 顶部添加 `DEPRECATED` 废弃声明，部署方案切换至裸机 PM2。
  - `apps/company-admin/vite.config.js` [MODIFY] — 将代理配置提取为 `apiProxy` 常量，同时注入 `preview.proxy`，解决生产环境 `vite preview` 不转发 API 请求的问题。
  - `apps/fis-hub/vite.config.js` [MODIFY] — 同上，新增 `preview.proxy` 并补充 `/api` rewrite 规则使路径对齐后端。
  - `backend/config.py` [MODIFY] — 新增 `CORS_ORIGINS` 环境变量（逗号分隔字符串），默认为本地 localhost 三端，生产环境可通过 `.env` 注入公网 IP。
  - `backend/main.py` [MODIFY] — 将硬编码的 `allow_origins` 列表改为从 `settings.CORS_ORIGINS` 动态解析，支持生产环境一行配置。
  - `.gitignore` [MODIFY] — 新增 `deploy/.env.production`（含 API Keys 和 SECRET_KEY）、`logs/`、`.next/` 的忽略规则，防止机密文件误提交。
  - `deploy/setup_server.sh` [CREATE] — Ubuntu 22.04 服务器一键初始化脚本（Python 3.11 / Node 20 / PM2 / Nginx）。
  - `deploy/ecosystem.config.js` [CREATE] — PM2 生态系统配置，统一管理 wonder-backend / wonder-hub / company-admin / fis-hub 四个进程，含日志路径、内存限制、重启策略。
  - `deploy/nginx.conf` [CREATE] — HTTP 公网 IP 版 Nginx 反向代理（80 端口主入口 → Wonder Hub + /api 后端；company-admin 和 fis-hub 直接暴露端口由 Vite preview 内置代理处理）。
  - `deploy/deploy.sh` [CREATE] — 日常代码更新脚本（git pull → pip install → pnpm build → pm2 reload，零停机）。
  - `deploy/.env.production` [CREATE] — 生产环境变量模板，含新生成的 64 位强 SECRET_KEY、CORS 公网 IP 占位符、绝对路径文件夹配置。
  - `deploy/README.md` [CREATE] — 完整生产运维手册（首次部署流程、日常更新、常用命令、数据库备份、防火墙端口清单）。
- **[Logic Reason]**:
  1. 放弃 Docker：SQLite 文件数据库与 Docker Volume 挂载存在 WAL 文件并发锁死风险（已在群晖阶段触发过崩溃），且轻量服务器 2-4GB RAM 无法承受多容器开销。PM2 无额外开销且对 Node.js + Python 混合场景支持完善。
  2. `preview.proxy` 修复：Vite 的 `server.proxy` 仅在 `vite dev` 模式生效，`vite preview` 需要独立的 `preview.proxy` 才能在生产模式下代理 /api 请求，否则 company-admin 和 fis-hub 的 API 调用会直接 404。
  3. CORS 动态化：生产环境公网 IP 在部署前未知，将 CORS 白名单提取为 `.env` 变量，用户只需填入服务器 IP 即可，无需修改代码。
- **[Pending/TODO]**: 用户需在阿里云控制台开放防火墙端口（22/80/5174/5175），并将 `deploy/.env.production` 中的 `YOUR_SERVER_IP` 替换为真实公网 IP 后，按照 `deploy/README.md` 执行首次部署流程。

### 2026-04-18 (Deployment - Ubuntu 22.04 & Local Backup)
- **[Status]**: 已完成
- **[Change Log]**:
  - `deploy/setup_server.sh` [MODIFY] —移除了强制安装 Python 3.11 及更新 `update-alternatives` 的逻辑，直接使用 Ubuntu 22.04 LTS 自带的 Python 3.10 及 python3-venv。补齐了 `sqlite3` 的安装。
  - `deploy/backup.sh` [CREATE] — 新增 SQLite 自动化热备脚本，利用 `sqlite3 .backup` 防锁死，支持 `gzip` 压缩并自动清理 30 天前的旧备份。
  - `deploy/README.md` [MODIFY] — 更新了数据库备份章节，加入了 `crontab` 每日定时备份的配置指南。
- **[Logic Reason]**: 用户确认使用经典的 Ubuntu 22.04 LTS 作为轻量应用服务器的底座，为了保证最大兼容性并符合“不折腾”原则，调整初始化脚本以匹配系统原生环境。同时针对“自动备份到本地且按月灵活付费”的需求，补充了定时备份脚本以替代昂贵的云盘快照功能。
- **[Pending/TODO]**: 按照 `deploy/README.md` 执行首次部署并在服务器上配置 `crontab`。

### 2026-04-18 (Bug Fix - 生产服务器后端启动崩溃：openpyxl 依赖缺失)
- **[Status]**: 已完成
- **[Change Log]**:
  - `backend/requirements.txt` [MODIFY] — 新增 `openpyxl>=3.1.0`，修复 `expenses.py` 的 Excel 导出依赖缺失问题。
- **[Logic Reason]**: 通过远程 HTTP 探测确认 wonder-hub 前端正常（200）但所有 `/api` 均 502，排除 nginx/vite proxy 问题，锁定后端进程宕机。通过查看 pm2 logs 截图，发现根因为 `ModuleNotFoundError: No module named 'openpyxl'`。该依赖被 `backend/routers/expenses.py` 使用，但从未加入 `requirements.txt`，导致服务器 `pip install -r requirements.txt` 时遗漏，uvicorn 启动即崩溃。
- **[Pending/TODO]**: 在服务器执行 `pip install openpyxl && pm2 restart wonder-backend` 后验证 `curl http://127.0.0.1:8000/health` 返回 200。

### 2026-04-18 (Bug Fix - FIS Hub 硬编码 localhost 导致云端登录失败)
- **[Status]**: 已完成
- **[Change Log]**:
  - `apps/fis-hub/src/App.jsx` [MODIFY] — 使用 `sed` 将全文件 37 处 `http://localhost:8000` 硬编码绝对 URL 批量替换为 `/api`（相对路径），以匹配 `vite.config.js` 中 `preview.proxy` 的代理转发规则。
- **[Logic Reason]**: 部署到阿里云后，FIS Hub 登录界面报"网络错误"。根因在于 `App.jsx` 的所有 API 调用均写死 `http://localhost:8000`——在用户浏览器中 `localhost` 指向用户自己的电脑而非服务器，自然无法连接。`vite.config.js` 已正确配置了 `preview.proxy`（`/api → http://127.0.0.1:8000`），将前端改为相对路径 `/api/...` 后代理即可在服务器侧正确转发。
- **[Pending/TODO]**: 需在服务器上重新执行 `pnpm build`（或 `./deploy/deploy.sh`）重建 fis-hub dist，然后 `pm2 reload fis-hub` 使新代码生效。

### 2026-04-18 (Bug Fix - 登录时密码验证触发 500 错误)
- **[Status]**: 已完成
- **[Change Log]**:
  - `backend/requirements.txt` [MODIFY] — 将 `passlib[bcrypt]==1.7.4` 拆分为 `passlib==1.7.4` 和 `bcrypt==3.2.2`。
- **[Logic Reason]**: 用户覆盖本地带历史数据的 `hk_admin.db` 后，登录接口 `/api/auth/login` 始终返回 500 错误。通过 PM2 日志定位到 `ValueError: password cannot be longer than 72 bytes, truncate manually if necessary`，这是 `passlib 1.7.4` 在搭配 `bcrypt 4.x.x` 版本时，验证哈希时触发的一个经典依赖兼容性 BUG。强制将 `bcrypt` 降级到 `3.2.2` 以绕过该漏洞。
- **[Pending/TODO]**: 在服务器更新依赖后测试真实登录。

### 2026-04-18 (Bug Fix - FIS Hub 登录后不显示组合数据)
- **[Status]**: 已完成
- **[Change Log]**:
  - 在服务器上执行 `pnpm build` 和 `pm2 reload all`，重建包括 FIS Hub、Company Admin 在内的所有前端微服务，使之前修改为 `/api/...` 的相对路径代理规则真正在生产环境生效。
- **[Logic Reason]**: 之前修复网络错误时，只是在 GitHub 和本地修改了 `App.jsx`，但服务器依然在静态伺服（serve）旧版含有硬编码 `localhost:8000` 的构建产物（`dist` 目录）。这导致用户虽然能够验证登录，但前端向 `http://localhost:8000/portfolios` 抓取数据时依然发生跨域和网络异常。执行构建后，服务器分发给浏览器的 JS 代码中不再有绝对路径。
- **[Pending/TODO]**: 检查前端页面的组合数据是否成功渲染。

### 2026-04-18 (Bug Fix - 组合删除功能失效修复)
- **[Status]**: 已完成
- **[Change Log]**:
  - `backend/routers/fip_router.py`: 在 `delete_portfolio` API 中补充了对 `portfolio_stats_cache`, `portfolio_history`, `broker_trades`, `sync_metadata` 四个关联表的数据清除，避免产生孤立垃圾数据。
  - `apps/fis-hub/src/App.jsx`: 修复了 `fetchPortfolios` 闭包状态未能获取到最新被清空的 `activeId`，从而导致删除后无法自动切换到首个组合导致白屏的问题。改用 React 的 `setActiveId(currentId => ...)` 函数式状态更新。
  - 在服务器上已重新 `git pull` 并 `pnpm build` 使全量代码生效。
- **[Logic Reason]**: SQLite 在当前配置下没有强制 `PRAGMA foreign_keys=ON`（仅限 SQLAlchemy，而在 FIP API 中通过 `sqlite3.connect` 直接连接没有开启），导致删除主组合后关联表未能级联删除。同时 React 的 `useEffect` 状态闭包陷阱使得组件未能及时反映删除导致的数据清空。
- **[Pending/TODO]**: 若在其它微服务中也用到类似的 `activeId` 同步机制，后续需要统一抽象成 Custom Hook 以防再次掉坑。

### 2026-04-18 (UI UX - FIS Hub Responsive Fluid Typography)
- **[Status]**: 已完成
- **[Change Log]**:
  - `apps/fis-hub/src/index.css` [MODIFY] — 在 `:root` 前面插入了针对 `html` 的响应式 `font-size` 配置（PC 端 `14px`，小屏设备 `<768px` 恢复为 `16px`）。
  - `apps/fis-hub/src/index.css` [MODIFY] — 编写 Python 脚本扫描并使用正则表达式将所有的 `px` 布局数值（padding, margin, width, top, border-radius 等，过滤掉 <= 4px 的边框值）全部按 `16px = 1rem` 比例转换为 `rem`。
  - `apps/fis-hub/src/App.css` [MODIFY] — 同上，也将遗留文件的像素单位转换为 `rem`。
- **[Logic Reason]**: 用户觉得目前 FIS-Hub 界面的字体过于庞大、不够精细。我们采用了“基准字号缩放配合 Rem 相对单位”的经典响应式打法：
  1. 将全局基准字号降至 14px，让整体视觉和字体变得更精细与紧凑。
  2. 利用 Rem 相对单位的特性，所有容器间距（如 `padding: 40px` 被转为了 `2.5rem`）在基准字号变为 14px 时会自动同比例等比缩小（变为实际 35px），实现用户所说的“间距随之调整”，整体版式完美协调不走样。
  3. 保留了移动端适配，一旦屏幕宽度小于 768px，基准字号自动恢复到 16px，所有容器间距和字体等比放大，保证在手机上的触控友好度和可读性。
- **[Pending/TODO]**: 可以在本地启动 `npm run dev` 在浏览器中拖拽改变窗口宽度，验收整体字号与间距的平滑切换效果。
- [Status]: 已完成
- [Change Log]: 
  - `routers/commissions.py` L230: 修复了 `confirm_ir56m` 查询时缺失 `company_id` 的严重越权漏洞（BUG-01）。
  - `routers/expenses.py`: 修复了多个接口缺乏认证或租户隔离漏洞（BUG-02 ~ 05）。引入 `UserCompanyAccess` 及 `_check_company_access` 方法，对 `list_expenses`, `stats_by_category`, `stats_by_fiscal_year`, `export_expenses`, `get_expense`, `update_expense`, `confirm_expense`, `reject_expense`, `delete_expense` 等接口增加了严格的 `company_id` 数据隔离验证。
- [Logic Reason]: 响应用户的要求：“要留意除了管理员之外，别的用户只能看到自己名下的公司，不能有信息泄露”。防止由于查询中没有过滤或者没有认证导致的跨公司数据泄露/修改。
- [Pending/TODO]: 前端/后端联调测试执行（用户侧），等待依据 `company_admin_test_plan.md` 进行实际操作。

### 2026-04-19 (Test Execution - Company Admin 自动化安全与功能测试)
- **[Status]**: 已完成
- **[Change Log]**:
  - `scratch/api_test_runner.py` [CREATE] — 编写全自动化 API 测试脚本，覆盖 11 个模块的核心 CRUD 操作及跨租户（T1-T7）安全渗透隔离测试。
  - `backend/routers/invoices.py` [MODIFY] — 修复 `create_invoice` 在写入 SQLite 时对 `uuid.UUID` 对象绑定失败报错 `500 Internal Server Error` 的问题（转为字符串）。
  - `backend/routers/expenses.py` [MODIFY] — 修复 `add_expense_manual` 缺失认证装饰器与租户鉴权的问题。
  - `backend/routers/expenses.py` [MODIFY] — 修复跨租户并发记账时 `voucher_number` 生成规则导致的 SQLite `UNIQUE constraint failed` 冲突。
- **[Logic Reason]**: 用户离开并授权系统自主完成全面的自动化回归与安全测试。通过执行脚本探测出在并发以及 SQLite 方言下的几处隐藏 API Bug。特别针对 UUID 以及全局唯一编号进行了容错处理。最后隔离测试（User B 访问 User A 资源）100% 返回 403 Forbidden，证明前期修复的隔离机制牢不可破。
- **[Pending/TODO]**: 后端全模块安全加固完成。待用户归来后，进行前端（UI界面对 403 的提示）与云端生产服务器代码的部署发布。

### 2026-04-19 (Test Execution - Wonder Platform 数据隔离与权限控制验收)
- **[Status]**: 已完成
- **[Change Log]**:
  - `backend/scripts/automated_test.py` [MODIFY] — 更新自动化测试脚本，修复了 `UserCreate` schema 的验证要求（发送 `name` 而不是 `display_name`）并支持兼容重复注册拦截时的 `422 Unprocessable Entity` 和 `400 Bad Request` 状态码。
  - `backend/routers/fip_router.py` [MODIFY] — 修复 `/admin/users/{user_id}` 系列接口（包含更新角色、状态以及删除用户），将其入参从 `int` 改正为匹配底层数据结构的 `str` (UUID)，解决了 API 在管理端抛出的 `422 validation error` 报错。
  - `apps/fis-hub/src/App.jsx` [MODIFY] — 修复 React 状态引发的白屏 Bug，移除了删除当前激活 Portfolio 时的强制 `setActiveId(null)`，由下一次拉取的列表完成柔性回退。
  - `apps/fis-hub/src/App.jsx` [MODIFY] — 补充 Auth 钩子：在用户登录成功及注销后，立刻调用 `fetchPortfolios()` 以刷新组合列表显示，从而解决登录后仍只能看到非登录视角的公有组合的渲染延迟问题。
  - `apps/fis-hub/src/components/AuthModal.jsx` [MODIFY] — 在前端注册的 Payload 组装处修正为传递 `name`，并将 `invited_by_advisor_id` 去掉无意义的 `Number()` 类型转换（顾问ID同样是UUID字符串）。
- **[Logic Reason]**: 执行由 24 项组成的完整测试时发现了底层参数类型的严格化冲突以及部分 UI 渲染延迟。统一调整以契合后端模型规范。并对删除动作进行解耦防止白屏。最终验证测试 24/24 全部通过。
- **[Pending/TODO]**: 整个数据层权限隔离已重塑完成并 100% 验证绿灯。后端代码可稳定在生产环境运行。建议将最终提交合并，并在云端服务器重新部署以交付完整可用版本。

### 2026-04-19 (Bug Fix - UI Portfolio Deletion Blue Screen)
- **[Status]**: 已完成
- **[Change Log]**:
  - `apps/fis-hub/src/App.jsx`: 修复了渲染时当 `data.details` 缺失时隐式返回 `undefined` 导致的 React 渲染崩溃（蓝屏空内容）的问题。为 `<PortfolioView>` 的渲染判断增加了显式的 Fallback 展示兜底逻辑。
  - `apps/fis-hub/src/App.jsx`: 修复了在组合被删除至空列表时，`activeId` 依然驻留为已删除 ID 的问题。通过在 `fetchPortfolios` 中增加 `if (list.length === 0) return null;` 的兜底，恢复了“No Portfolio Selected”页面的正常呈现。
- **[Logic Reason]**: 发现前端 React Ternary 表达式语法陷阱 (`A && B && <C>`)。如果表达式结果为 `undefined`，React 组件直接渲染空白。此现象结合深色背景最终呈现为所谓的“蓝屏”。通过将隐式判断改为明确的三元运算符 `? :`，极大增强了前端应对空数据、后端拉取异常或数据清空等竞态条件的抗风险与鲁棒能力。
- **[Pending/TODO]**: 线上已经执行 `deploy.sh` 进行了无宕机更新，可请用户尝试在云端重现该删除测试组合的流程，验证稳定性。
  - `apps/fis-hub/src/App.jsx`: 进一步修复了在替换代码时不慎移除了 `!activeId` 的空状态判定，导致 `<main>` 容器被完全清空的问题。现已重新恢复 `Select a portfolio from the sidebar or create a new one` 占位符的正确流转。

[Status]：已完成
[Change Log]：
1. `backend/routers/fip_router.py`:
   - `NewPortfolioRequest` (L97): 增加 `is_public` 字段。
   - `create_portfolio` (L345+): 提取当前 `user_id`，在插入 `portfolios` 表时记录 `user_id` 和 `is_public`。
   - `list_portfolios` (L328+): 在拉取组合时，除公开组合外，附加 `OR user_id=?` 条件，确保非管理员用户可以看到自己创建的私有组合。
2. `apps/fis-hub/src/App.jsx` (L135, L353, L944):
   - 初始化 `newPf` 时增加 `is_public: false`。
   - 提交创建时将 `is_public` 传入 API。
3. `apps/fis-hub/src/components/modals/CreatePortfolioModal.jsx` (L65+):
   - 在弹窗表单中增加 `is_public` (公开可见) 的 Checkbox 复选框。
4. `apps/fis-hub/src/components/AuthModal.jsx` (L116+):
   - 修复了新用户注册时卡在“未收到登录凭证”的Bug，补充了注册成功后自动调用 `/auth/login` 获取 token 的逻辑。
[Logic Reason]：
1. 组合不可见原因：此前创建组合时未写入 `user_id` 且默认 `is_public=0`。这导致普通用户创建组合后，接口因权限过滤将该组合隐藏，导致侧边栏无法显示自己创建的组合。通过绑定归属关系并提供公开选项，恢复了正常的数据可见性。
2. 注册卡死原因：原后端 `/auth/register` 只返回用户实体不返回 token，前端在强制校验 token 时报错。通过在前端增加自动 Login 流程解决。
[Pending/TODO]：目前系统运行流畅，等待用户的本地全流程功能测试反馈。

[Status]：已完成 (Delete Modal Crash Fix)
[Change Log]：
1. `apps/fis-hub/src/components/modals/DeleteModal.jsx` (L1-4):
   - 补全了遗漏的 `Trash2` 图标组件导入 (`import { Trash2 } from 'lucide-react';`)。
   - 在 Props 中增加了缺失的 `setDeleteCandidate` 参数。
2. `apps/fis-hub/src/App.jsx` (L1113):
   - 将 `setDeleteCandidate` 状态 setter 传递给 `DeleteModal` 组件。
[Logic Reason]：
1. 删除崩溃原因：因为上个版本将 Modal 拆分到单独文件时，遗漏了引用 `lucide-react` 中的 `Trash2` 图标。当用户点击侧边栏的删除图标试图弹出“确认删除”的对话框时，React 因找不到 `Trash2` 抛出 ReferenceError 异常，导致渲染树崩溃（触发整页报错）。补充导入后修复。
2. 传递参数：取消删除操作时需要清空 `deleteCandidate` 状态，因此必须在父组件正确传递 setter 函数。
[Pending/TODO]：检查还有没有其他 Modal 组件漏了传递参数。

[Status]：已完成 (Registration & Advisor Management Refactoring)
[Change Log]：
1. `apps/fis-hub/src/components/AuthModal.jsx`:
   - 移除了注册表单中的“普通用户 / 付费用户”账号类型选择。
   - 移除了注册表单中的“选择您的专属顾问”下拉框。
   - 更新了向后端发送的数据 Payload，移除了 `invited_by_advisor_id`，所有新用户注册统一为默认的普通账户（free role）。
2. `backend/routers/fip_router.py`:
   - 新增了 `UpdateUserAdvisorRequest` 数据验证模型。
   - 添加了 PUT `/admin/users/{user_id}/advisor` 接口，允许管理员给用户分配或取消分配专属顾问。当分配顾问时，如果当前是 `free` 角色则会自动升级为 `premium`；取消顾问时则降级回 `free`。
   - 更新了 `admin_list_users` 接口的返回数据，附带了 `advisor_id` 字段以供前端绑定 Select 控件。
3. `apps/fis-hub/src/components/AdminPanel.jsx`:
   - 在管理员页面的列表中新增了分配/更改“关联顾问”的 `<select>` 下拉框控件。
   - 新增 `handleAdvisorChange` 逻辑，在管理员切换下拉框时，调用上述后端的 PUT 接口以更新顾问关系及用户级别。
[Logic Reason]：
用户要求改变系统的工作流：让用户注册时保持简单（全部是普通账户），并且由后台管理员在管理面板来决定谁是财务顾问，并手工为普通用户分配顾问（这会自动将普通用户升级为付费模式以获得定制服务）。这样更符合 B 端 SaaS 业务的后台管控逻辑。
[Pending/TODO]：无。

[Status]：已完成 (Admin Advisor Client Panel Crash Fix)
[Change Log]：
1. `backend/routers/fip_router.py`:
   - 修复了 `advisor_list_clients` 接口导致 HTTP 500 崩溃的 SQL 查询问题：因为 `users` 表里没有 `last_login_at` 字段，使用了 `u.created_at` 替代。
   - 修复了管理员身份访问此接口时的逻辑缺陷。之前该接口总是强制通过 `advisor_id = current_user.id` 过滤，导致管理员身份只能查出空列表。现在当 `role == "admin"` 时，取消了 advisor_id 限制，且额外关联查出了 `advisor_name` 以便于展示。
2. `apps/fis-hub/src/components/AdvisorClientsPanel.jsx`:
   - 如果当前用户是管理员，将在客户名字旁边用 Badge 展示该客户所关联的财务顾问名称。
[Logic Reason]：
用户反馈在管理员页面中点击“顾问客户管理”页面报 HTTP 500 错误。分析发现是因为接口在处理 Admin 权限和读取缺失数据库字段时抛出异常导致。修复后不仅解决了崩溃，还真正补全了“上帝视角”的管理员逻辑。
[Pending/TODO]：无。

[Status]：已完成 (Advisor Create Portfolio For Client)
[Change Log]：
1. `backend/routers/fip_router.py`:
   - 修改了 `NewPortfolioRequest` 数据模型，增加了可选的 `client_id` 字段。
   - 更新了 `create_portfolio` 接口逻辑：如果请求体中包含 `client_id`，且当前登录用户是 Admin 或分配给该客户的 Advisor（通过 `assert_advisor_owns_client` 验证），则会将新建的投资组合挂载到该 `client_id` 名下，而不是创建在当前登录的顾问/管理员名下。
2. `apps/fis-hub/src/App.jsx`:
   - 在 `handleCreatePortfolio` 中，将 `client_id` 附加到发送给后端的 payload 中。
   - 为 `<AdvisorClientsPanel>` 传递了 `onAssignPortfolio` 回调函数，当被调用时，会预填充 `client_id` 并打开“新建组合”弹窗。
3. `apps/fis-hub/src/components/AdvisorClientsPanel.jsx`:
   - 在客户列表面板中，每个客户行的右侧新增了显眼的 `+ 创建组合` 绿色按钮。顾问或管理员可以直接点击该按钮，为该客户代办建立投资组合。
[Logic Reason]：
用户（以顾问或管理员身份）需要为旗下的付费客户代客理财，代替客户直接在后台生成投资组合。通过允许后端接收并重写 `user_id`，并在前端客户管理页面提供快捷入口，打通了“代客建仓”的业务流。
[Pending/TODO]：目前代建组合后，左侧的侧边栏不会显示客户的组合（侧边栏仅显示自己的组合），这是预期的，顾问需要点击“查看组合”来展开客户的组合列表。如果有需要，后续可以考虑增加全局通知或自动刷新局部列表的机制。

[Status]：已完成 (Fix 422 Unprocessable Entity for Client Portfolios)
[Change Log]：
1. `backend/routers/fip_router.py`:
   - 修正了 `advisor_get_client_portfolios` 接口中 `client_id` 的类型声明，从 `int` 改为 `str`（因为数据库中 user_id 已经是 UUID 字符串）。由于之前的 `int` 类型限制，当前端请求传入 UUID 时，FastAPI 底层的 Pydantic 验证直接拦截并抛出 `422 Unprocessable Entity`，导致前端捕获错误后静默显示为“暂无投资组合”。
   - 同步修正了 `CreateAdvisorClientRequest` 及 `advisor_upload_insurance_excel` 中的 `client_id` 和 `advisor_id` 类型声明，避免后续功能出现同样的解析错误。
[Logic Reason]：
用户反馈前端在“我的客户”面板中看到该客户已经有 2 个组合，但是点开下拉列表后却显示“暂无投资组合”和报错 500。排查发现是由于 `client_id` 参数的类型注解与数据库类型不匹配导致的 FastAPI 隐式拦截。
[Pending/TODO]：无。

[Status]：已完成 (UI Cleanup)
[Change Log]：
1. `apps/fis-hub/src/components/Sidebar.jsx`:
   - 注释 / 移除了 "Import File / Screenshot" 和 "Sync from Broker" 这两个暂未实现功能的占位按钮。
[Logic Reason]：
这两个功能暂不具备完整后端支撑，暴露在 UI 上会引起用户的疑惑和误操作，因此应用户要求先行清理，让侧边栏只保留核心的 "New Strategy" 新建按钮。
[Pending/TODO]：后续当多模态收据识别和券商 API 同步功能开发完毕后，可再行恢复这两个按钮。

[Status]：已完成 (Restore & Fix Dividend Features)
[Change Log]：
1. 回退了上一笔隐藏分红管理按钮的 Git 提交，重新把 "Dividends" 和 "Add Dividend" 放出来了。
2. `apps/fis-hub/src/App.jsx` 和 `apps/fis-hub/src/components/modals/DividendModal.jsx`:
   - 修复了 `DividendModal` 未传入 `data` (activePortfolio) 属性导致的 `Cannot read properties of undefined (reading 'filter')` 的蓝屏奔溃。
3. `apps/fis-hub/src/components/modals/ManageDivModal.jsx`:
   - 修复了因为未从 `lucide-react` 导入 `Trash2` 图标而引发的组件渲染崩溃问题。
4. `apps/fis-hub/src/App.jsx`:
   - 为相关接口补充了 `authHeaders()` 以确保在鉴权开启的环境下依然可访问。
[Logic Reason]：
应用户要求必须保留该重要功能，因此深入排查了点击报错“蓝屏”的真实原因（两个 React 组件级的低级错误：缺少 prop 注入与缺少组件引用），修正后即可正常提供完整的 Dividend 分红数据写入/查看功能。
[Pending/TODO]：无。

---
## 2026-04-19 Session 2 — 结果页全面升级 + 欢迎页 + 三案例讨论

**[Status]** 已完成

**[Change Log]**
- `questionnaire/page.tsx`：新增 WelcomeScreen 组件（隐私/免责/问卷说明），showWelcome 状态控制首屏；Q12 医疗险选项更新为内地+香港双适配
- `result/page.tsx`：全面重写
  - PV折现引擎（10年内3.5%，10年以上5.5%）替代简单复利
  - 三个典型客户案例数据（REFERENCE_CASES）：王先生/陈小姐/李先生夫妇
  - 案例按 matchTemplates 自动高亮与当前用户画像匹配的案例
  - 每个案例包含：缺口测算+公式、组合构建（含香港储蓄分红险/投连险/终身寿险）、保障架构、顾问洞察
  - 配置理由区块（ALLOCATION_RATIONALE）含储蓄险角色说明
  - 预约顾问区块新增微信按钮（#wechat-contact）
  - FIS 深链接映射到公开示范组合（Income/Accumulation）

**[Logic Reason]**
- PV折现法比简单复利更符合专业财务规划逻辑，短期保守/长期积极的折现率分层符合实际配置预期
- 三案例覆盖「积累期家庭/专业人士/传承期高净值」三个典型生命周期，与顾问实际客群高度匹配
- 李先生案例的投连险+终身寿险结构准确反映香港传承规划的主流工具

**[Pending/TODO]**
- FIS Hub 端可考虑解析 URL 参数 `portfolio` 自动加载对应示范组合
- 微信号 WonderWealth_HK 为占位符，需替换为真实微信ID或二维码
- WhatsApp 号码 +85212345678 需替换为真实号码

---
## 2026-04-19 Session 2 (Update) — 案例数据进一步精调

**[Status]** 已完成

**[Change Log]**
- `result/page.tsx`：移除全局的 `AllocationBar` 和 `ALLOCATION_RATIONALE`，改用纯 case-by-case 展示。
- `result/page.tsx` (Case A)：将储蓄险调整为“高保证成分保单/固定收益基金”，将投资物业调整为“变现释放600万重配高息债/REIT”，资产结构全面转向高流动高收益。
- `result/page.tsx` (Case B)：无传承需求，退休提取率上调至 5.5%，资产池降至 880万（加入2%通胀计算）；补充明确的“提前3年(47岁)退休”论证；在顾问洞察加入自动扣款与保险年缴“强制储蓄”的论述。
- `result/page.tsx` (Case C)：净资产增加到 3000万（含1000万房产），月开支确认为 8万/月。添加了终身寿险赔付作为内地房产潜在遗产税“专属现金储备/税费拨备”的深度洞察，避免子女未来被迫折价卖房换现。

**[Logic Reason]**
- 删除抽象资产配置框架，采用更真实生动的人物画像驱动。
- Case A 强调盘活低收益固定资产（房产），提高资金利用率。
- Case B 考虑实际通胀影响并量化“躺平”年份，为单身人士提供强大的行动动力（提早退休）。
- Case C 的修改精准切中了内地高净值人士跨境资产配置中最核心痛点：房产遗产税的流动性危机，极大提升了终身寿险（杠杆+流动性）的销售逻辑。

**[Pending/TODO]**
- 无

---
## 2026-04-19 Session 3 — 细节打磨与联络信息更新

**[Status]** 已完成

**[Change Log]**
- `questionnaire/page.tsx`：在医疗选项中将文案从“内地社保医疗”改为“仅有内地医保”，明确区分公费医保与商业险。
- `questionnaire/page.tsx`：在子女教育（目标就读地区）选项中新增“中国内地”选项。
- `result/page.tsx`：修复 `pv` 函数在 `0.035 * 100` 时造成的浮点数精度爆炸显示错误（例如 3.500000004%），硬编码返回保留小数的数字。
- `result/page.tsx`：底部联络预约区移除了 WhatsApp 选项，全宽双列排版展示“微信咨询 (aniuwu224)” 和“返回中心”。

**[Logic Reason]**
- 匹配内地客户偏好，明确“内地医保”和“中国内地”留学标签。
- 修复浮点错误提升金融级页面的专业信任度。
- 将联络渠道收束至微信并指定明确微信号，形成核心的高转化私域闭环（符合 case by case 诊断设计的核心目的）。

**[Pending/TODO]**
- 整体端到端测试，准备生产环境部署。

---
## 2026-04-19 Session 4 — 后台通知通道激活

**[Status]** 已完成

**[Change Log]**
- `backend/.env`：更新了 `TELEGRAM_BOT_TOKEN` 和 `TELEGRAM_ADMIN_CHAT_ID`。

**[Logic Reason]**
- 填入用户提供的真实 Telegram 凭据，使问卷提交后能通过 Telegram Bot 实时推送诊断摘要。

**[Pending/TODO]**
- 等待用户提供 Gmail 相关配置，完成邮件备份通知通道的设置。

---
## 2026-04-19 Session 5 — 通知通道全激活 (Telegram + Gmail)

**[Status]** 已完成

**[Change Log]**
- `backend/.env`：更新了 `GMAIL_SENDER`、`GMAIL_APP_PASSWORD` 和 `ADMIN_EMAIL`。
- 确认 Telegram 和 Email 配置均已就绪。

**[Logic Reason]**
- 完成了所有通知通道的配置。现在问卷提交后会通过 Telegram 即时通知，并发送一份 HTML 格式的诊断报告到管理员邮箱 `helloaniuwu@gmail.com` 作为备份。

**[Pending/TODO]**
- 准备进行最终的上线联调测试。

---
## 2026-04-19 Session 6 — 通知内容深度增强

**[Status]** 已完成

**[Change Log]**
- `backend/routers/assessment_router.py`：
    - 在 `send_telegram` 中新增了 `📝 原始问卷明细` 模块，通过分类图标排版展示了家庭、财务、资产、保障等所有原始数据。
    - 在 `send_email` 中新增了 `📝 原始问卷回答汇总` 列表，采用更专业的 HTML 邮件样式展示完整数据。
    - 为两个通知通道增加了显式的成功/失败日志记录。

**[Logic Reason]**
- 顾问在收到提醒后，需要立即掌握客户的原始财务细节，而不仅仅是诊断结论，以便在后续联系中更有针对性。

**[Pending/TODO]**
- 交付用户。

### 2026-04-19 FIS 组合越权漏洞修复
- **[Status]**：已完成
- **[Change Log]**：修改 `backend/routers/fip_router.py`，实现 `assert_portfolio_access(portfolio_id, user)` 并注入到 9 个涉及组合及交易修改/删除的路由中 (`update_portfolio`, `delete_portfolio`, 等)。
- **[Logic Reason]**：封堵了未授权修改和删除他人组合的安全漏洞。通过依赖注入 `get_current_user`，强制要求执行操作的用户必须是组合所有者，或者是 `admin` / `advisor` 角色。
- **[Pending/TODO]**：暂无。前端对接及其他业务逻辑待续。

### 2026-04-19 FIS 投顾权限隔离优化
- **[Status]**：已完成
- **[Change Log]**：修改 `backend/routers/fip_router.py`，更新了 `list_portfolios` 和 `assert_portfolio_access` 方法。引入了针对 `advisor_clients` 表的查询。
- **[Logic Reason]**：进一步收束 `advisor` 的权限范围。之前的实现中 `advisor` 拥有全局的读写权限。现改为：`advisor` 在列表中只能看到自己名下绑定的 `client_id` 创建的组合（以及公开组合和自己的组合），并且在修改和删除时，也通过查询 `advisor_clients` 严格校验客户归属权。
- **[Pending/TODO]**：暂无。前端对接及其他业务逻辑待续。

### 2026-04-19 前端操作按钮权限管控
- **[Status]**：已完成
- **[Change Log]**：修改了 `apps/fis-hub/src/App.jsx`、`Sidebar.jsx` 和 `PortfolioView.jsx`。
- **[Logic Reason]**：根据用户的权限隔离需求，我们在前端 `App.jsx` 中新增了 `canEditPortfolio` 的逻辑判断。仅当满足以下条件之一时，用户才有编辑权限：1) 是 admin；2) 是组合的创建者；3) 是 advisor，且该组合是非公开的他人组合（后端已确保其为投顾客户的组合）。如果不具备权限，组合列表中的删除图标、以及详情页里的“重命名、调仓、撤销交易、组合成分修改、添加/管理分红”等写入类按钮，均会被隐藏。
- **[Pending/TODO]**：暂无。

### 2026-04-19 修复编辑/删除时因缺失 Token 导致的拦截问题
- **[Status]**：已完成
- **[Change Log]**：修改了 `apps/fis-hub/src/App.jsx`，为 `handleRename`、`handleDelete`、`handleUndoConfirmed`、`handleRebalanceExecute`、`handleCompSave` 等一系列前端发起的修改组合接口，补齐了缺失的 `authHeaders()` 请求头。
- **[Logic Reason]**：之前在后端加入了鉴权强依赖 (`Depends(get_current_user)`) 后，如果没有附带合法的 Token，即使前端判定可以操作，当网络请求到达后端时也会因 `401 Unauthorized` 或 `403` 被拒绝。现在已修复这个问题，前后端鉴权链路完全贯通。
- **[Pending/TODO]**：暂无。
